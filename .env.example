# ==========================================
# Zepp Life (原小米运动) 自动刷步数环境变量配置文件模板
# 使用方法：复制本文件重命名为 .env，并填入你的实际配置
# ==========================================

# 1. 账号与密码（必填）
# 支持单个账号，或多个账号用 # 分隔（例如：13800000000#abc@qq.com）
USER=13800000000
PWD=your_password

# 2. 步数随机范围设置（选填，默认 18000 ~ 25000）
MIN_STEP=18000
MAX_STEP=25000

# 3. 本地加密密钥（选填）
# 用于加密保存 Token 及已绑定设备 ID，加速后续登录；必须是 16 个字符
# 请务必使用随机生成的密钥，切勿使用弱密码（密文会提交到仓库，弱钥可被爆破）
# 生成示例：python -c "import secrets,string;print(''.join(secrets.choice(string.ascii_letters+string.digits) for _ in range(16)))"
AES_KEY=CHANGE_ME_TO_RANDOM_16_CHARS

# 4. 多账号执行配置（选填）
SLEEP_GAP=5
USE_CONCURRENT=False

# 5. 推送服务通知配置（选填）
# PushPlus 推送配置
PUSH_PLUS_TOKEN=
PUSH_PLUS_HOUR=
PUSH_PLUS_MAX=30

# 企业微信 Webhook 机器人推送配置
PUSH_WECHAT_WEBHOOK_KEY=

# Telegram Bot 推送配置
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# 6. JSON 格式全量配置（选填）
# GitHub Actions 环境中使用的 CONFIG 变量范例
# CONFIG='{"USER": "13800000000", "PWD": "your_password", "MIN_STEP": "18000", "MAX_STEP": "25000"}'
